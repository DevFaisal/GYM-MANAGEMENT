const RevealMe = ({ config, origin }) => {
    config = {
        origin: origin,
        duration: 100,
        delay: 250,
        distance: "500px",
        scale: 1,
        easing: "ease",
    };
}

export default RevealMe;